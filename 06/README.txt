Parses and Assembles asm code and converts it to binary machine code.
Generates a new .hack file that contains the binary machine code
Main is the driver file, in your terminal navigate to the project 6 directory, and type "./main ./max/Max.asm" (or any other folder / file contained in the directory)
